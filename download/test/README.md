# 업데이터 시험용 — 끝나면 지울 것

2026-09-11. 앱 안에서 바로 업데이트가 실제로 도는지 확인하려고 잠깐 둔 것이다.
**진짜 배포가 아니다.** 아무 데서도 링크하지 않는다.

- `version.json` — 1.1.0 이 있다고 알린다 (시험 빌드만 이 주소를 본다)
- `Nooki-Setup.exe` — 그 1.1.0 실물. 서명이 없는 exe 이므로 오래 두지 않는다

시험이 끝나면 `download/test/` 폴더째 지운다.
